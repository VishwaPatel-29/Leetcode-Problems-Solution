# 🔹 509. Fibonacci Number

## 🧠 Problem Statement
The Fibonacci numbers, commonly denoted as `F(n)`, form a sequence where each number is the sum of the two preceding ones.

The sequence starts with:

```cpp
F(0) = 0
F(1) = 1
```

For `n > 1`:

```cpp
F(n) = F(n - 1) + F(n - 2)
```

Given an integer `n`, return the value of `F(n)`.

---

## 📌 Examples

```cpp
Input: n = 2
Output: 1

Explanation:
F(2) = F(1) + F(0)
     = 1 + 0
     = 1
```

```cpp
Input: n = 3
Output: 2

Explanation:
F(3) = F(2) + F(1)
     = 1 + 1
     = 2
```

```cpp
Input: n = 4
Output: 3

Explanation:
F(4) = F(3) + F(2)
     = 2 + 1
     = 3
```

---

## 💡 Approach
- Handle the base cases:
  - If `n == 0` → return `0`
  - If `n == 1` → return `1`
- Use two variables:
  - `a` → previous Fibonacci number
  - `b` → current Fibonacci number
- Loop from `2` to `n`
- Calculate next Fibonacci number using:
  - `c = a + b`
- Update values:
  - `a = b`
  - `b = c`
- Return `b`

---

## ⚡ C++ Solution

```cpp
class Solution {
public:
    int fib(int n) {

        if(n == 0){
            return 0;
        }

        if(n == 1){
            return 1;
        }

        int a = 0;
        int b = 1;

        for(int i = 2; i <= n; i++){

            int c = a + b;

            a = b;
            b = c;
        }

        return b;
    }
};
```

---

## 🧪 Dry Run

```cpp
n = 5

a = 0
b = 1
```

### First Iteration (i = 2)

```cpp
c = a + b = 0 + 1 = 1

a = 1
b = 1
```

### Second Iteration (i = 3)

```cpp
c = a + b = 1 + 1 = 2

a = 1
b = 2
```

### Third Iteration (i = 4)

```cpp
c = a + b = 1 + 2 = 3

a = 2
b = 3
```

### Fourth Iteration (i = 5)

```cpp
c = a + b = 2 + 3 = 5

a = 3
b = 5
```

### Final Answer

```cpp
F(5) = 5
```

---

## 🔢 Fibonacci Sequence

```cpp
Index : 0 1 2 3 4 5 6  7  8
Value : 0 1 1 2 3 5 8 13 21
```

---

## 🚀 Notes
- Time Complexity: `O(n)`
- Space Complexity: `O(1)`

---

## 🏷️ Tags
`Math` `Dynamic Programming` `Recursion` `Easy`
