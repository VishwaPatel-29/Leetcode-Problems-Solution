# 🔹 1837. Sum of Digits in Base K

## 🧠 Problem Statement
Given an integer `n` (in base 10) and a base `k`, return the sum of the digits of `n` after converting it from base 10 to base `k`.

After conversion, each digit is treated as a normal base 10 number.

---

## 📌 Examples

```
Input:  n = 34, k = 6
Output: 9

Explanation:
34 in base 10 becomes 54 in base 6.

5 + 4 = 9
```

```
Input:  n = 10, k = 10
Output: 1

Explanation:
10 is already in base 10.

1 + 0 = 1
```

---

## 💡 Approach
- Convert the number into base `k`
- Use:
  - `n % k` → to get the last digit
  - `n / k` → to remove the last digit
- Add all digits into `sum`
- Return the final sum

---

## ⚡ C++ Solution 

```cpp
class Solution {
public:
    int sumBase(int n, int k) {

        int sum = 0;

        while(n > 0){

            int digit = n % k;

            sum += digit;

            n = n / k;
        }

        return sum;
    }
};
```

---

## 🧪 Dry Run

```cpp
n = 34
k = 6

34 / 6 = 5  -> remainder = 4
5 / 6 = 0   -> remainder = 5

Base 6 number = 54

Sum = 5 + 4 = 9
```

```cpp
n = 10
k = 2

10 % 2 = 0 -> sum = 0
10 / 2 = 5

5 % 2 = 1 -> sum = 1
5 / 2 = 2

2 % 2 = 0 -> sum = 1
2 / 2 = 1

1 % 2 = 1 -> sum = 2
1 / 2 = 0

Final Answer = 2
```

---

## 🚀 Notes
- Time Complexity: O(logₖ n)  
- Space Complexity: O(1)

---

## 🏷️ Tags
`Math` `Easy`
