# 🔹 412. Fizz Buzz

## 🧠 Problem Statement
Given an integer `n`, return a string array `answer` where:

- `"FizzBuzz"` if the number is divisible by both `3` and `5`
- `"Fizz"` if the number is divisible by `3`
- `"Buzz"` if the number is divisible by `5`
- Otherwise, store the number itself as a string

---

## 📌 Examples

```
Input:  n = 3
Output: ["1","2","Fizz"]
```

```
Input:  n = 5
Output: ["1","2","Fizz","4","Buzz"]
```

```
Input:  n = 15
Output: ["1","2","Fizz","4","Buzz","Fizz","7","8","Fizz","Buzz","11","Fizz","13","14","FizzBuzz"]
```

---

## 💡 Approach
- Traverse numbers from `1` to `n`
- Check conditions in order:
  - divisible by both `3` and `5` → `"FizzBuzz"`
  - divisible by `3` → `"Fizz"`
  - divisible by `5` → `"Buzz"`
  - otherwise convert number to string
- Store each result in the answer array

---

## ⚡ C++ Solution

```cpp
class Solution {
public:
    vector<string> fizzBuzz(int n) {

        vector<string> ans;

        for(int i = 1; i <= n; i++){

            if(i % 3 == 0 && i % 5 == 0){
                ans.push_back("FizzBuzz");
            }
            else if(i % 3 == 0){
                ans.push_back("Fizz");
            }
            else if(i % 5 == 0){
                ans.push_back("Buzz");
            }
            else{
                ans.push_back(to_string(i));
            }
        }

        return ans;
    }
};
```

---

## 🧪 Dry Run

```cpp
n = 5

1 -> not divisible by 3 or 5 -> store "1"
2 -> not divisible by 3 or 5 -> store "2"
3 -> divisible by 3         -> store "Fizz"
4 -> not divisible by 3 or 5 -> store "4"
5 -> divisible by 5         -> store "Buzz"

Final Output:
["1","2","Fizz","4","Buzz"]
```

---

## 🚀 Notes
- Time Complexity: O(n)  
- Space Complexity: O(n)

---

## 🏷️ Tags
`Array` `String` `Math` `Easy`
