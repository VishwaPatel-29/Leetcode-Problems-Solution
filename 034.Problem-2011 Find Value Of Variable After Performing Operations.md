# 🔹 2011. Final Value of Variable After Performing Operations

## 🧠 Problem Statement
There is a variable `X` initially equal to `0`.

You are given an array `operations` containing operations:

- `"++X"` or `"X++"` → increment `X` by `1`
- `"--X"` or `"X--"` → decrement `X` by `1`

Return the final value of `X` after performing all operations.

---

## 📌 Examples

```
Input:  operations = ["--X","X++","X++"]
Output: 1

Explanation:

Initially:
X = 0

--X -> X = -1
X++ -> X = 0
X++ -> X = 1
```

```
Input:  operations = ["++X","++X","X++"]
Output: 3
```

```
Input:  operations = ["X++","++X","--X","X--"]
Output: 0
```

---

## 💡 Approach
- Start with `x = 0`
- Traverse every operation
- If the operation increases `X`, increment `x`
- Otherwise decrement `x`
- Return the final value

---

## ⚡ C++ Solution 

```cpp
class Solution {
public:
    int finalValueAfterOperations(vector<string>& operations) {

        int x = 0;

        for(string op : operations){

            if(op == "++X" || op == "X++"){

                x++;
            }
            else{

                x--;
            }
        }

        return x;
    }
};
```

---

## 🧪 Dry Run

```cpp
operations = ["X++","++X","--X","X--"]

Initially:
x = 0

First Operation:
"X++"
x = 1

Second Operation:
"++X"
x = 2

Third Operation:
"--X"
x = 1

Fourth Operation:
"X--"
x = 0

Final Answer = 0
```

---

## 🚀 Notes
- Time Complexity: O(n)  
- Space Complexity: O(1)

---

## 🏷️ Tags
`Array` `String` `Simulation` `Easy`
