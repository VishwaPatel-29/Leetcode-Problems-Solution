# 🔹 1920. Build Array from Permutation

## 🧠 Problem Statement
Given a zero-based permutation array `nums`, build an array `ans` of the same length such that:

```cpp
ans[i] = nums[nums[i]]
```

for every:

```cpp
0 <= i < nums.length
```

Return the array `ans`.

A zero-based permutation means:
- All integers are unique
- Values range from `0` to `nums.length - 1`

---

## 📌 Examples

```cpp
Input: nums = [0,2,1,5,3,4]

Output: [0,1,2,4,5,3]
```

### Explanation

```cpp
ans[0] = nums[nums[0]] = nums[0] = 0
ans[1] = nums[nums[1]] = nums[2] = 1
ans[2] = nums[nums[2]] = nums[1] = 2
ans[3] = nums[nums[3]] = nums[5] = 4
ans[4] = nums[nums[4]] = nums[3] = 5
ans[5] = nums[nums[5]] = nums[4] = 3
```

```cpp
Final Answer:
[0,1,2,4,5,3]
```

---

```cpp
Input: nums = [5,0,1,2,3,4]

Output: [4,5,0,1,2,3]
```

---

## 💡 Approach
- Create an empty array `ans`
- Traverse the array using loop
- For every index `i`:
  - Find `nums[i]`
  - Use that value as a new index
  - Push `nums[nums[i]]` into `ans`
- Return `ans`

---

## ⚡ C++ Solution

```cpp
class Solution {
public:
    vector<int> buildArray(vector<int>& nums) {

        vector<int> ans;

        for(int i = 0; i < nums.size(); i++){

            ans.push_back(nums[nums[i]]);
        }

        return ans;
    }
};
```

---

## 🧪 Dry Run

### Example

```cpp
nums = [0,2,1,5,3,4]
```

---

### Index 0

```cpp
nums[0] = 0

ans[0] = nums[nums[0]]
       = nums[0]
       = 0
```

```cpp
ans = [0]
```

---

### Index 1

```cpp
nums[1] = 2

ans[1] = nums[nums[1]]
       = nums[2]
       = 1
```

```cpp
ans = [0,1]
```

---

### Index 2

```cpp
nums[2] = 1

ans[2] = nums[nums[2]]
       = nums[1]
       = 2
```

```cpp
ans = [0,1,2]
```

---

### Index 3

```cpp
nums[3] = 5

ans[3] = nums[5]
       = 4
```

```cpp
ans = [0,1,2,4]
```

---

### Index 4

```cpp
nums[4] = 3

ans[4] = nums[3]
       = 5
```

```cpp
ans = [0,1,2,4,5]
```

---

### Index 5

```cpp
nums[5] = 4

ans[5] = nums[4]
       = 3
```

```cpp
ans = [0,1,2,4,5,3]
```

---

## ✅ Final Output

```cpp
[0,1,2,4,5,3]
```

---

## 🚀 Notes
- Time Complexity: `O(n)`
- Space Complexity: `O(n)`

---

## 🏷️ Tags
`Array` `Simulation` `Easy`
