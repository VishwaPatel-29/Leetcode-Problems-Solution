# 🔹 1295. Find Numbers with Even Number of Digits

## 🧠 Problem Statement
Given an array `nums` of integers, return how many numbers contain an even number of digits.

---

## 📌 Examples

```
Input:  nums = [12,345,2,6,7896]
Output: 2

Explanation:
12   -> 2 digits  -> even
345  -> 3 digits  -> odd
2    -> 1 digit   -> odd
6    -> 1 digit   -> odd
7896 -> 4 digits  -> even

Only 12 and 7896 contain an even number of digits.
```

```
Input:  nums = [555,901,482,1771]
Output: 1

Explanation:
1771 contains 4 digits,
which is even.
```

---

## 💡 Approach
- Traverse each number in the array
- Count how many digits the number contains
- Check if the digit count is even
- If yes, increase the answer count
- Return the final count

---

## ⚡ C++ Solution 

```cpp
class Solution {
public:
    int findNumbers(vector<int>& nums) {

        int count = 0;

        for(int num : nums){

            int digits = 0;

            int temp = num;

            while(temp > 0){

                digits++;

                temp = temp / 10;
            }

            if(digits % 2 == 0){
                count++;
            }
        }

        return count;
    }
};
```

---

## 🧪 Dry Run

```cpp
nums = [10,555,1000]

First Number:
10 -> 2 digits -> even

Second Number:
555 -> 3 digits -> odd

Third Number:
1000 -> 4 digits -> even

Answer = 2
```

---

## 🚀 Notes
- Time Complexity: O(n × d)  
  (`d` = number of digits)

- Space Complexity: O(1)

---

## 🏷️ Tags
`Array` `Math` `Easy`
